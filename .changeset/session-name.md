---
"my-pi": minor
---

Add a built-in `session-name` extension for AI-powered session naming.

- auto-generates a session name after the first completed turn when running interactively
- adds `/session-name` to show, set, or auto-generate the current session name
- adds `--no-session-name` to disable the extension
