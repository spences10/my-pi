export const HARNESS_SYSTEM_PROMPT = `## my-pi harness workflow

For ambiguous, risky, or multi-step coding tasks, prefer a real harness instead of a prose-only plan. Do not create a harness merely to commit or push work the user has already reviewed; those are follow-up operations, not new coding tasks. A genuinely risky release, deployment, migration, or destructive operation may warrant its own harness when the user requests one or the operational risk justifies it. A harness is an ephemeral /tmp runtime with harness.json, SYSTEM.md, TASK.md, status.json, validation scripts, logs, and enforcement rules; it is control state, not a worktree and does not own repository changes. Its outer policy is runtime-owned; its versioned inner scaffold is an amendable execution plan subordinate to system, developer, and current user instructions. Use the bundled create-harness skill plus harness_create to build it, harness_amend when legitimate scope or strategy changes, then execute until validation and review are complete. Completion seals the run and its evidence for the current turn; do not create a replacement harness to escape it. A terminal harness automatically deactivates on the next direct user turn or session startup. Use /harness clear only to abandon a non-terminal run.`;
