{
	"name": "@spences10/pi-observability",
	"version": "0.0.2",
	"description": "Optional live event stream and local web dashboard for Pi agent sessions",
	"keywords": [
		"observability",
		"pi",
		"pi-package",
		"sse",
		"telemetry"
	],
	"homepage": "https://github.com/spences10/my-pi/tree/main/packages/pi-observability#readme",
	"license": "MIT",
	"author": "Scott Spence <me@scottspence.com>",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/spences10/my-pi.git",
		"directory": "packages/pi-observability"
	},
	"bin": {
		"pi-observability-server": "./dist/server.js"
	},
	"files": [
		"dist",
		"README.md"
	],
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		},
		"./server": {
			"types": "./dist/server.d.ts",
			"default": "./dist/server.js"
		}
	},
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "pnpm --filter \"$npm_package_name^...\" run build:self && pnpm run build:self",
		"build:self": "vp exec tsc -p tsconfig.build.json && cp src/schema.sql src/dashboard.html src/dashboard.css dist/ && mkdir -p dist/fonts && cp node_modules/@fontsource/victor-mono/files/victor-mono-latin-400-normal.woff2 node_modules/@fontsource/victor-mono/files/victor-mono-latin-700-normal.woff2 dist/fonts/",
		"check": "pnpm --filter \"$npm_package_name^...\" run build:self && pnpm run check:self",
		"check:self": "vp exec tsc --noEmit -p tsconfig.json",
		"test": "pnpm --filter \"$npm_package_name^...\" run build:self && pnpm run test:self",
		"test:self": "vp exec vitest run",
		"coverage:self": "vp exec vitest run --coverage",
		"coverage": "pnpm --filter \"$npm_package_name^...\" run build:self && pnpm run coverage:self"
	},
	"dependencies": {
		"@fontsource/victor-mono": "^5.2.8"
	},
	"devDependencies": {
		"@earendil-works/pi-coding-agent": "catalog:",
		"@types/node": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	},
	"peerDependencies": {
		"@earendil-works/pi-coding-agent": "*"
	},
	"engines": {
		"node": ">=24.15.0"
	},
	"pi": {
		"extensions": [
			"./dist/index.js"
		],
		"image": "https://raw.githubusercontent.com/spences10/my-pi/main/assets/pi-package-preview.png"
	}
}
